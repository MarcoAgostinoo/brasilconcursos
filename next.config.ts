/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'plus.unsplash.com', 'us-west-2.graphassets.com'], // Adicionando ambos os domínios do Unsplash
  },
};

module.exports = nextConfig;
